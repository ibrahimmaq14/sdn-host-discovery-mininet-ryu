# sdn-host-discovery-mininet-ryu
SDN Host Discovery using Mininet and Ryu controller. Detects hosts dynamically, maintains MAC-to-port mapping, installs OpenFlow rules, and handles packet_in events. Demonstrates controller-switch interaction, traffic forwarding/filtering, and performance testing using ping and iperf.
